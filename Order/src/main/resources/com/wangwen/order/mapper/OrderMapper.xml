<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangwen.order.mapper.OrderMapper">
    <insert id="generateOrder">
            INSERT INTO `order` (order_id, order_price, user_id,cinema_id, movie_id,
		create_time,update_time) VALUES
        (0,12,#{userId},'1','1',now(),now())
    </insert>
</mapper>
